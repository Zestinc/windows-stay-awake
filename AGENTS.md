# Agent guide

## 定位

在 Windows 本机运行的单文件工具：关闭全部自动休眠 / 自动关屏 / 自动锁屏，
让机器保持唤醒并持续显示当前界面。无依赖，Windows PowerShell 5.1 起可用。

**保持成独立的单文件工具**：`-Guard` 与 `-DisableLockScreen` 需要常驻进程并放宽
锁屏策略，任何要求「无常驻、不弱化 UAC、每次改动发哈希锁定新版本」的分发方式都
容不下它。控制这台 Windows 的远程通道是 Moonlight 串流，与本工具无关。

## 怎么跑 / 怎么验证

- 本地只能做静态检查；真实行为一律靠 `.github/workflows/ci.yml`，在
  windows-2022 与 windows-2025 两个真 Windows 上跑 apply → verify → restore。
- 改完必须等 CI 全绿，并且**去日志里确认断言的成功标记真的打印了**。
  这个 repo 出现过两次假通过，见下。

## 硬约束（违反会静默出错，不是风格问题）

- **`StayAwake.ps1` 必须带 UTF-8 BOM。** PS 5.1 读无 BOM 的 UTF-8 文件会按系统
  ANSI 代码页解码，中文被拆成乱码字节后撑坏字符串边界，整个脚本解析失败。
  CI 有字节级断言守着。任何编辑工具改完都要复查前三字节是 `EF BB BF`。
- **`.cmd` 文件里任何一行都不许出现 `&` 或 `|`，注释也不行。** `rem` 不保护它们，
  cmd 会把注释拆成多条命令执行。这个错在本 repo 犯过两次（一次在 CI 的 rem 里，
  一次在启动器的 rem 里）。合法例外只有 `2>&1` 这类重定向语法。
- **在 .cmd 里调用另一个 .cmd 必须加 `call`。** 否则控制权不返回，后面所有行
  静默跳过——CI 曾因此有个测试从未执行却一直显示绿色。
- **提权判断只能在 `StayAwake.ps1` 里做**，不许挪回启动器。批处理只能对命令行做
  字符串匹配来猜，那份猜测会跟脚本真正的参数集解析漂移；而把参数经 `echo` 管道
  传递会让含 `&` 的参数被当命令执行。CI 有静态守卫。
- **所有 `powercfg` 调用走 `Invoke-PowerCfg`。** `ErrorActionPreference='Stop'` 之下
  原生命令写 stderr 会被升级成终止异常，`$LASTEXITCODE` 根本轮不到检查。
- **回滚恢复的是「有效值」，不是注册表键的存在性。** powercfg 没有「取消设置」，
  删掉键之后 `/setactive` 会立刻按当前生效值把它重建。方案键缺项时向下回落到
  方案默认值，两侧（AC/DC）必须各自独立回落。
- **`backup-pristine.json` 只创建一次，永不覆盖。** 连续 Apply 会把普通备份刷成
  已被改过的中间态，原值就永久丢了。覆盖面扩大时只补录新增项。
- **PowerShell 函数返回集合会被展开**，空集合变成 `$null`，`$null.Count` 在
  StrictMode 下抛错。接收处一律 `@(...)` 包裹。
- 数值字面量：`0x80000000` 会被当成 Int32 解析成负数，要写 `0x80000000L`。

## 验证的诚实边界

- **真实 UAC 弹窗在任何 CI 上都测不到**：runner 自身就是管理员，提权分支跑不到。
- 组策略冲突、多用户登录、域环境这几条只有检测和报告，没有 CI 覆盖。
- 家里那台 Windows 只经 Moonlight 串流操作（没有 SSH）；它不在线时无法实机验证。

## 当前状态

功能完整，CI 全绿。经过一轮 Codex（gpt-6-astra）全量 review 并修完 12 个问题。
公开分发：public repo + gist，README 是唯一权威说明。
